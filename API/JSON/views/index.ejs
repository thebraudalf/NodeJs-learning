<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">


    <% if (locals.recipeData) { %>

      <h2 id="recipeTitle">
        <%= recipeData.name %>
      </h2>

      <h3>Ingredients:</h3>

      <ul id="ingredientsList">
        <li>
          <%= recipeData.ingredients.protein.name %>, <%= recipeData.ingredients.protein.preparation %>
        </li>
        <li>
          <%= recipeData.ingredients.salsa.name %>
        </li>

        <% recipeData.ingredients.toppings.forEach(toppingData=> { %>

          <li>
            <%= toppingData.quantity %> of <%= toppingData.name %>
          </li>

          <% }); %>
      </ul>

      <% } else { %>
        <h2>Pick your favourite taco ingredient 👆</h2>
        <% } %>


  </div>
</body>

</html>